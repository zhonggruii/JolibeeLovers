# ONLY redirect root URL to login (no !)
/       /login.html    302

# Allow direct access to ALL HTML files
/*.html     /:splat.html   200

# Allow ALL assets (CSS/JS/images)
/*.css      /:splat.css    200
/*.js       /:splat.js     200
/*.png      /:splat.png    200
/*.jpg      /:splat.jpg    200